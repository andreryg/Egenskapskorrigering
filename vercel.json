{
    "builds": [
        {
            "src": "api/app.py",
            "use": "@vercel/python",
            "config": {
                "includeFiles": ["createJSON.py"]
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/app.py"
        }
    ]
}
